<nav class="header-categories d-flex flex-center">
  <div class="icons">
    <%= link_to  category_record_path(@category, @record) do %>
      <i class="fa-solid fa-arrow-left back"></i>
    <% end %>
  </div>
  <h1 class="transaction-header-nav">Editing Transaction</h1>
  <input type="submit" name="commit" value="Save" class="login-button new-edit-cat" form="new-tran-from" data-disable-with="Log in">
</nav>

<%= render "form", category: @category, record: @record %>
