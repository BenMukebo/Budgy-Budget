<p style="color: green"><%= notice %></p>

<% render @category %>

<article class="category">
  <div class="d-flex">
    <div class="image">
      <%= image_tag(@category.image) %>
    </div>
    <div class="name-date d-flex">
      <h3 class="name"><%= @category.name %></h3>
    </div>
  </div>
  <br>
  <p>Is about building a mobile web application where you can manage your budget: you have a list of transactions associated with a category, so that you can see how much money you spent and on what.<p/>
  <br>
  <hr>
  <br>
  <p class="date d-flex"> <%= @category.created_at.strftime('%Y') %>
    <span><%= @category.created_at.to_formatted_s(:short) %></span>
  </p>
  <div class="total-payment d-flex flex-center">
    <p class="">TOTAL PAYMENT</p>
    <p class="total-amount">Tolal: $ </p>
  </div>

  <div class="d-flex flex-center">
    <div class="action-btn">
      <%= link_to "Edit category", edit_category_path(@category) %>
    </div>
    <div class="action-btn">
      <%= button_to "Destroy category", @category, method: :delete %>
    </div>
  </div>

  <div class="d-flex flex-center">
    <div class="action-btn">
      <%= link_to "Add new transaction", new_category_record_path(@category.id), class: ""%>
    </div>
    <div class="back-btn">
      <%= link_to "Back", categories_path %>
    </div>
  <ul>

    <% @category.records.each do |record| %>
      <li id="categories">
      
        <h2> hello wordl </h2>
        <%= render category %>
  
      </li>
  <%end%>
  </ul>

  <h2> <%= @category.records.map{|record| record} %> </h2>


</article>

